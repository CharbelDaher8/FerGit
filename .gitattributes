# Keep LF in the repository and in checkouts regardless of core.autocrlf, so snapshot files
# and generated bindings compare byte-for-byte on every platform.
* text=auto eol=lf
*.png binary
*.ico binary
*.icns binary
