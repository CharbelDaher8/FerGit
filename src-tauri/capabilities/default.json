{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: FerGit's own commands, event listening, the folder picker, and setting its own light/dark theme. No fs/shell/http access from the webview.",
  "windows": ["main"],
  "permissions": ["core:event:default", "core:window:allow-set-theme", "dialog:allow-open"]
}
