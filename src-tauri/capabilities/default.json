{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: FerGit's own commands, event listening, and the folder picker. No fs/shell/http access from the webview.",
  "windows": ["main"],
  "permissions": ["core:event:default", "dialog:allow-open"]
}
